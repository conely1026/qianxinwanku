# Repository instructions

## 文档维护

- 开始需求、架构或发布流程相关改动前，先阅读 [MAP.md](MAP.md) 以及其中对应的权威文档。
- `MAP.md` 是文档根入口；新增权威文档后，必须更新 `MAP.md` 和对应的 `docs/*/index.md`。
- `docs/` 下的新文档只能进入 `requirements/`、`development/`、`exec_plan/` 或 `research/`。确有新分类需求时，先更新 `MAP.md` 的目录职责。
- `docs/` 下每份文档必须包含 `status`、`owner`、`last_verified`、`sources` YAML 元数据，以及标题后的 `文档入口` 段落。
- 从 `MAP.md` 开始，任何权威文档必须在两次点击内到达；提交前检查所有相对链接。
- 同一主题只保留一个权威来源。需求写目标和验收，开发文档写架构与约束，执行计划写步骤和门槛，研究文档写盘点证据。
- 代码改动使文档中的现状、接口、路径或验证结论失效时，同一个提交内更新权威文档和 `last_verified`。
- `README.md` 只保留项目介绍、功能概览、快速运行和文档入口，不堆放详细设计。
- 临时讨论记录、构建输出和自动生成报告不能替代权威文档；需要长期保留的结论必须归档到对应分类。
